'use client'
import './sidebar.scss'
import { useState, useRef } from 'react'

const navItems = ["home", "settings", "backup", "mail", "cloud"];

export const Sidebar = () => {

    return (
        <h1>hello</h1>
    );
};